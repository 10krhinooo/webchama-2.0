parent=keycloak

styles=css/login.css

# Fallback "back to app" URL used only when the current client has no
# baseUrl configured (client.baseUrl is preferred, see template.ftl).
# Point this at the real frontend origin per environment.
frontendUrl=http://localhost:5173
